import { FC } from "react";
import { Layout } from "../../components/Layout";

/**
 * Course page
 *
 * @returns JSX component
 */
export const Course: FC = () => {
  return (
    <Layout>
      <div></div>
    </Layout>
  );
};
